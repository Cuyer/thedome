ktor {
    deployment {
        # Port is configured via the PORT environment variable
        port = ${?PORT}
    }
    application {
        modules = [ pl.cuyer.thedome.ApplicationKt.module ]
    }
}
